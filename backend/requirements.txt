fastapi>=0.128
uvicorn[standard]>=0.39
pydantic>=2.13
python-multipart>=0.0.9
httpx>=0.27
lancedb>=0.14
pyarrow>=21.0
# ONNX Runtime — base CPU+CoreML+CUDA EPs on mac/linux. On Windows the CPU
# wheel ships DirectML alongside, but we pin the explicit -directml package
# so DmlExecutionProvider is always present without a CUDA toolkit install.
onnxruntime>=1.18; platform_system != "Windows"
onnxruntime-directml>=1.18; platform_system == "Windows"
# Whisper via faster-whisper (CTranslate2 backend, no torch).
faster-whisper>=1.0
# Windows GPU for CTranslate2: cuDNN 9 runtime DLLs. CT2 4.5+ ships
# cublas + cudart statically inside _ext.pyd; cuDNN 9 is the only
# external runtime dep, and the wheel is a pure-data wheel (~687 MB)
# so pip install is fast + reliable on the CI Win runner.
# Without this CTranslate2.get_cuda_device_count() returns 0 even on
# RTX cards and faster-whisper silently uses CPU+int8.
nvidia-cudnn-cu12==9.22.0.52 ; sys_platform == "win32"
# OCR via RapidOCR — ONNX models ship inside the wheel, no separate download.
rapidocr-onnxruntime>=1.3
# Model fetch + tokenizer for CLIP ONNX bundle.
huggingface-hub>=0.24
# Xet storage transport for HF Hub. Without it, repos that ship via
# Xet (Systran/faster-whisper-*, Xenova/clip-* — most non-trivial ones
# now) fall back to vanilla HTTP via cas-bridge.xethub.hf.co which
# stalls mid-download on flaky links. End-user symptom: progress bar
# stuck at e.g. 141/500 MB and never advances.
hf_xet>=1.0
tokenizers>=0.20
# Video segmentation (pure-Python, depends on numpy+click — no torch).
scenedetect>=0.6.7
imageio-ffmpeg>=0.6
pillow>=11.1
pillow-heif>=1.1
# numpy 2.x removes np.NaN which lancedb 0.14 relies on. Pin <2 until upstream catches up.
numpy>=1.26,<2
# opencv >=4.11 declares numpy>=2 as a hard dependency on Python>=3.9, which
# conflicts with lancedb's numpy<2 need.
opencv-python-headless>=4.9,<4.11
